name: Sync Site Content

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: sync-events
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Sync events from Google Calendar
        env:
          GCAL_API_KEY: ${{ secrets.GCAL_API_KEY }}
        run: node scripts/sync-events.mjs

      - name: Sync photos from the approved-gallery Drive folder
        env:
          GCAL_API_KEY: ${{ secrets.GCAL_API_KEY }}
        run: node scripts/sync-gallery.mjs
        continue-on-error: true # a Drive hiccup here should never block the calendar sync below

      - name: Commit events.json / gallery.json if either changed
        run: |
          if git diff --quiet -- events.json gallery.json; then
            echo "No changes to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add events.json gallery.json
          git commit -m "Sync events and gallery from Google Calendar / Drive [skip ci]"
          git push
